/**
 * Default Pixi Application
 * Doesnt have any special methods for now..
 */
export default class Application  extends PIXI.Application{
    constructor(options = {}){
        super(options);
    }
}